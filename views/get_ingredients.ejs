<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="styles.css">
    <title>Document</title>
</head>
<body>
    
    <h1>Get Ingredients Needed</h1>
    
    <div class="form-control form-style">
        <form action="/ingredients" method="POST">

            <label>Start Week:</label>
            <input type="number" name="startWeek" />

            <label>Start Day:</label>
            <select name="startDay">
              <option value="Monday">Monday</option>
              <option value="Tuesday">Tuesday</option>
              <option value="Wednesday">Wednesday</option>
              <option value="Thursday">Thursday</option>
              <option value="Friday">Friday</option>
              <option value="Saturday">Saturday</option>
              <option value="Sunday">Sunday</option>
            </select>

            <label>End Week:</label>
            <input type="number" name="endWeek" />
          
            <label>End Day:</label>
            <select name="endDay">
                <option value="Monday">Monday</option>
                <option value="Tuesday">Tuesday</option>
                <option value="Wednesday">Wednesday</option>
                <option value="Thursday">Thursday</option>
                <option value="Friday">Friday</option>
                <option value="Saturday">Saturday</option>
                <option value="Sunday">Sunday</option>
            </select>
          
            <button type="submit">Get Ingredients</button>
          </form>
    </div>

    <!-- <pre><%= JSON.stringify(ingredients, null, 2) %></pre> -->

    <% if (error) { %>
        <p style="color: red;"><%= error %></p>
    <% } %>

    <% if (ingredients) { %>

        <h2>Ingredients Needed for <% if(startWeek === endWeek && startDay === endDay){ %>
            <%= startDay %>, Week <%= startWeek %>
        <% }else if(startWeek === endWeek) { %>
            <%= startDay %> - <%= endDay %>, Week <%= startWeek %>
        <% }else{ %>
            <%= startDay %>, Week <%= startWeek %> - <%= endDay %>, Week <%= endWeek %>
        <% } %>  
             </h2>

             <div class="container">
        
                <ul>
                    <button class="button dairy">Dairy</button>
                    <% ingredients.forEach(ingredient => { %>
                        <% if(ingredient.group === 'dairy') { %> 
                        <li id="dairy" class="hidden">
                            <%= ingredient.name %>: 
                            <%= ingredient.quantity %> 
                            <%= ingredient.unit %>
                        </li>
                        <% } %>
                    <% }) %>
                </ul>
                
                <ul>
                    <button class="button seasoning">Seasoning</button>
                    <% ingredients.forEach(ingredient => { %>
                        <% if(ingredient.group === 'seasoning') { %> 
                        <li id="seasoning" class="hidden">
                            <%= ingredient.name %>: 
                            <%= ingredient.quantity %> 
                            <%= ingredient.unit %>
                        </li>
                        <% } %>
                    <% }) %>
                </ul>
        </div>
        <div class="container">
        
        <ul>
            <button class="button fresh">Fresh</button>
            <% ingredients.forEach(ingredient => { %>
                <% if(ingredient.group === 'fresh') { %> 
                <li id="fresh">
                    <%= ingredient.name %>: 
                    <%= ingredient.quantity %> 
                    <%= ingredient.unit %>
                </li>
                <% } %>
            <% }) %>
        </ul>
        
        <ul>
            <button class="button frozen">Frozen</button>
            <% ingredients.forEach(ingredient => { %>
                <% if(ingredient.group === 'frozen') { %> 
                <li id="frozen">
                    <%= ingredient.name %>: 
                    <%= ingredient.quantity %> 
                    <%= ingredient.unit %>
                </li>
                <% } %>
            <% }) %>
        </ul>
        
        <ul>
            <button class="button pantry">Pantry</button>
            <% ingredients.forEach(ingredient => { %>
                <% if(ingredient.group === 'pantry') { %> 
                <li id="pantry">
                    <%= ingredient.name %>: 
                    <%= ingredient.quantity %> 
                    <%= ingredient.unit %>
                </li>
                <% } %>
            <% }) %>
        </ul>
        
        <ul>
            <button class="button produce">Produce</button>
            <% ingredients.forEach(ingredient => { %>
                <% if(ingredient.group === 'produce') { %> 
                <li id="produce">
                    <%= ingredient.name %>: 
                    <%= ingredient.quantity %> 
                    <%= ingredient.unit %>
                </li>
                <% } %>
            <% }) %>
        </ul>
    </div>
    <% } %>

    <script src="/main.js"></script>
</body>
</html>